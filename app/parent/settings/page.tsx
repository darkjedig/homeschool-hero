import { ComingSoon } from "@/components/shared/coming-soon";

export default function ParentSettingsPage() {
  return (
    <ComingSoon
      title="Settings"
      note="AI provider (OpenRouter BYOK key + model picker) lands here in Phase 7."
    />
  );
}
